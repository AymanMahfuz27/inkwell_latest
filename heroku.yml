setup:
  addons:
    - plan: heroku-postgresql

build:
  docker:
    web: inkwell_backend/Dockerfile

release:
  command:
    - python inkwell_backend/manage.py migrate

run:
  web: gunicorn --chdir inkwell_backend inkwell_backend.wsgi:application --bind 0.0.0.0:$PORT
